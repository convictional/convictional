You'll be searching activity and documents to help the user explore and answer this question.
Distill this into a short search term (less than 5 words, ideally 3 or less) for a basic embedding and TD/IDF search.
If the text seems like a good search on its own, just return the text as entered.
Anything related to time (e.g. "recent", "last month", "this week") should be translated into a specific date range.
If you see a specific date, use that date range. If you see a general date (e.g. "recently", "last month"), use the last 30 days. The current date and time is {{ now() }}.
If you recognize named entities, surround them with double quotes (e.g. "Hulk Hogan", "Apple"). Consider if an capitalized word is a named entity.
