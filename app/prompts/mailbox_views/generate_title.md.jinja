{% include "users/_system_prompt.md.jinja" %}

Generate a short, descriptive title (3-6 words) for a custom mailbox view based on this request:

"{{ view_request }}"

Return only the title, without quotes or punctuation at the end.
