// Must match the id of the floating-ui portal mount container in the app
// layout; FloatingPortal and that container have to point at the same id.
export const FLOATING_PORTAL_ROOT_ID = "react-floating-portal-root"
